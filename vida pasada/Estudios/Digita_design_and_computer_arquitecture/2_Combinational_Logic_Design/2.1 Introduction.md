>[!note] Circuit
>Is a network that processes discrete-valued variables. A circuit can be viewed as a black box.

It has:
-> One or more discrete-valued *input terminals*.
-> One or more discrete-valued *output terminals*
-> a *functional specification* describing the relationship between inputs and outputs.
-> a *timing specification* describing the delay between inputs changing and outputs responding.
An *element* is itself a circuit with inputs, outputs and a specification
A *node* is a wire, whose voltage conveys a discrete-valued variable.
Nodes are classified as: *input, output, or internal*.
Wires that are not inputs or outputs are called internal nodes.
![[Circuits_black_box.png]]
In figure 2.2, there are three elements, and six nodes. A,B,C are inputs. Y and Z are outputs. $n_{1}$ is an internal node between $E_{1}$ and $E_{3}$.
___
Digital circuits are classified as *combinational* or *sequential*. For example, a logic gate is a combinational circuit. 
A sequential circuit outputs depend on both current and previous values of the inputs; In other words, it depends on the input sequence.
A combinational circuit is *memoryless*, but a sequential circuit has memory.
Here we'll se combinational circuit.
I recommend see the figures 2.3 -2.6 to understand the concept.
To simplify srawings, we often use a single line with a slah through it and a number next to it to indicate a *bus*, a bundle of multiple signals.
The number in the slash specifies how many signals are in the bus.
>[!tip] Circuit 
>A circuit is combinational if it consists of interconnected circuit elements such that
>-> Every circuit element is itself combinational.
>-> Every node of the circuit is either designated as an input to the circuit or connects to exactly one output terminal of a circuit element
>-> The circuit contains no cyclic paths: every path through the circuit visits each circuit node at most once.

![[Combinational circuits.png]]

In this image, a, c, e are combinational circuits, the rest no. Explanation in the book. p.55
