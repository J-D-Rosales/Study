<span style="color:yellow">In general, sequential circuits include all circuits that are not combinational</span>
It means, whose whose output cannot be determined simply by looking at the current input. 
Since they are kooky we'll study some of them, and discipline ourselves to synchronous sequential circuits, to analyze and design sequential systems.
# 3.3.1 Some problematic circuits.
<span style="color:	#87CEEB">Example 3.3 Astable circuits</span>
The output of the next circuit is fed back to the first inverter, say that the propagation delay is 1 ns. 
![[Screenshot from 2025-09-10 12-15-56.png]]
As you see, in each iteration the value on each inverter will change. The circuit has no stable states and is said to be **unstable or astable**. Each node oscillates between 0 and 1 with a *period* of 6ns. This is called a **ring oscilator**.
![[Screenshot from 2025-09-10 12-17-48.png]]
## RACE Conditions
A race condition is simply when you have a logic gate that goes faster than other in a important part. Therefore, it could get stuck because there was no time to other to answer properly.
Let's take this circuit.
![[Screenshot from 2025-09-10 12-41-13.png]]
I have a referencial image, but don't worry, I'll explain it, it's easy.
![[Screenshot from 2025-09-10 12-41-52.png]]
Now, FIrst let's take, CLK = D = 1. THen, Q = 1, because the value D is going to be passed. That is, store one bit of information. Now, let's put CLK falls (CLK = 0). The latch should remember his last value, i mean, 1. Q sohould be 1, right?
Well, it depends, if the NOT gate is taking a lot of time, see what's gonna happen.
SInce CLK = 0, N1 = 0. Since, $\overline{CLK}$ is not 1 stil, beause the NOT gate is taking so much time, it is 0 stiil. THen N2 is 0. NOw, N1 = 0, and N2 = 0, THen Q = 0, NOw, don't matter what you do, N2 will be 0, in a infinitive loop, It means Q = 0, and will not be stored properly.
This is an example of *asynchronous circuit* design in which outputs are directly fed back to inputs. Asynchronous circuits are infamous for having race conditions where the behavior of the circuit depends on which of two paths through logic gates is faster.
# 3.3.2 Synchronous Sequential Circuits
The examples contain loops called **cyclic paths**, in which outputs are fed directly back to inputs. They are sequential rather than combinational.
To avoid such problems in synchronous sequential circuits designers break the cyclic paths by inserting registers somewhere in the path.
The registers contain the state of the system, which changes only at the clock edge, so we say the state is synchronized to the clock. If the clock is sufficiently slow, so that the inputs to all registers settle before the next clock edge, all races are eliminated.
Knowing this we recall to a formal definition.
Recall that a circuit is defined by its input and output terminals and its functional and timing specifications. 
<span style="color:skyblue">A sequential circuit has a finite set of discrete states</span> $\{ S_{0},S_{1},\dots,S_{k-1} \}$. A *synchronous sequential* circuit has a clock input, whose rising edges indicate a sequence of times at which state transitions occur.
We often use the terms *current state* and *next state* to distinguish the state of the system at the present from the state to which it will enter on the next clock edge.
Here, the time specification consists of an upper bound, $t_{pcq}$ , and a lower bound, $t_{ccq}$, on the time from the rising edge of the clock until the output chanches, as wall as *setup* and *hold times*, $t_{setup}$ and $t_{hold}$, that indicate when the inputs must be statble relative to the rising edge of the clock.
Now, let's see the rules of sychronous sequential circuit composition.
- Every circuit element is either a register or a combinational circuit
- At least one circuit element is a register
- All registers receive the same clock signal
- Every cyclic path contains at least one register
A flip-flop, is the simplest synchronoes sequential circuit.
![[Screenshot from 2025-09-10 19-32-20.png]]
IN this case the prime after $S$ indicates next state, not inversion. 
# 3.3.3 Synchronous and Asynchronous Circuits
Asynchronoes circuits are more general than synchronous design. Despite decades of research on asynchronous circuitsl virtually all digital systems are essentially synchronous.
However, asynchronous circuits are occasionally necessary and by the investigation it seems that will help us improving out designs.
# 3.4 Finite state machines
![[Screenshot from 2025-09-10 19-39-43.png]]
This forms of drawn synchronous sequential circuits are called **finite state machines (FSMs)**. 
A circuit with $k$ registers can be in one of a finite number $(2^{k})$ unique states.
An FSM has $M$ inputs, $N$ outputs, and $k$ bits of state. It also receives a clock and, optionally, a reset signal.
His combinational components are:
- Next state logic
- output logic
- register that store the state
On each clock edge, the FSM advances to the next state, which was computed based on the current state and inputs.
Based on their functional specification we can classified the machines:
**Moore machines** and **Mealy machines**.
IN mealy machines, the outputs depend on both the current state and the current inputs. 
# 3.4.1 FSM Design Example
It is interesting to say, that it is easy to desing this. But, you need to keep track of the theory. I'll try my best.
Imaging trying to solve the problem of traffic.
![[Screenshot from 2025-09-10 22-09-57.png]]
So, to solve the problem, a kid installed two traffic sensors, which just tell us it there is or no traffic (easy right?). Let's called it $T_{A}$ and $T_{B}$. You could see that on the image.
Sensor says true if students are present and False other way. 
Now, we also have installed Traffic lights, $L_{A}$ and $L_{B}$, to control traffic. BY the input it should say if it shoud stay turn to green or red. 
Then:
FSM has two inputs $T_{A}$ and $T_{B}$, and two outputs $L_{A}$ and $L_{B}$, you've got it right? I hope so.
Well, let's say we provide a clock with a 5 secods period, on each clock tick (rising edge) the lights MAY CHANGE. And last but not least the reset button, which just reset the mechanism.
Now, how could we do the *state transition diagram?*
BUT WAIT, We need to stablish what is initial, our system knows nothing, so let's say that:
**initial state(reset)**: the lights are green on Academic Ave, and red on Bravado Blvd. Every 5 seconds the control examines the traffic pattern and decides what to do next.
Since we are lazy, Let's put out transition diagram (You could analyze that it's correc by the way).
![[Screenshot from 2025-09-10 22-20-29.png]]
The there is no longer traffic on Academic Ave, the light on Academic wave will change yellow for 5 seconds, and after that red, changing Blvd's light green. The same analogy the other way.
We do not bother to add the clock, you'll thank me later. Because, the clock simply controls when the transitions should occur, but the diagram already does that.
The diagram is sort of an automaton, It means, recieves and input and decide wheter to pass or not to other state. ALso, there is Reset at the start, because we don't want to know what has happend before the reset was pressed.
If there is no symbol on the arc, it accepts everything.
Ok, we have the states diagram, now we want to have *state transition diagram*, because, it will be easy to construct form there.
![[Screenshot from 2025-09-10 22-28-10.png]]
The first table just indicates, for each state and input what the next state $S'$ would be. We use Dont care symbols whenever the next state does not depend on a particular input.
Also, reset is omitted from the table. Insteat we use resettable flip-flops that always fo to state $S0$ on reset, independent on the input.
To build a real circuit, the states and outputs must be assigned *binary encodings*. Kid chooses the simple encoding, because he's a kid. Where the bits needed will be log2(n) techo. So, if I have to encode 15 states, then I will need  4 bits, because if i choose 3, I'll have just 8 forms to represent the ecoding. 
Each state and each output is encoded with two bits:$S_{1:0},L_{A_{1}:0}$ and $L_{B1:0}$.
So $S 0 \to 00$
Where JUST FOR THE STATE TRANSITION TABLE IWTH BINARY ENCODINGS.
![[Screenshot from 2025-09-10 22-38-50.png]]
The revised state transition table is a truth table specifying the next state logic. It defines next state, $S'$, as a function of the current state, $S$ and the inputs.
Then , we can used K-Maps to get the bookean expression. Which is:
$$
\begin{align}
S_{1}' &  =  \bar{S_{1}}S_{0} + S_{1} \bar{S_{0}} \bar{T_{B}} +S_{1} \bar{S_{0}} T_{B} \\
S_{0}'  & = \bar{S_{1}} \bar{ S_{0}}   \bar{T_{A}} + S_{1} \bar{S_{0}} \bar{T_{B}}
\end{align}
$$

$S_{1}'$ could be reduced to a XOR operation says the book.
$$
\begin{align}
S_{1}'  & = S_{1} \oplus S_{0} \\
S_{0}'  & = \bar{S_{1}} \bar{S_{0}} \bar{T_{A}} + S_{1} \bar{S_{0}} \bar{T_{B}}
\end{align}
$$
Now, We have the input, we need the output, or no light will appear. 
![[Screenshot from 2025-09-10 22-47-03.png]]
If you want to do your K-MAP, good for you, the equations are the next:
$$
\begin{align}
L_{A 1}  & = S_{1} \\
L_{A 0}  & = \bar{S_{1}} S_{0} \\
L_{B 1}  & = \bar{S_{1}} \\
L_{B 0}  &  = S_{1} S_{0}
\end{align}
$$
Now, we need the template, which we already have it a MOORE FSM. jsjsj.
WE draw the 2-bit state register. On each clock edge, the state register copies the next state, $S_{1:0}'$, to become the state $S_{1:0}$.The state register receives a synchronous or asynchronous reset to initialize the FSM at startup. Then, he draws the next state logic, by the equation.
Let's see the diagram, which is really a template, wehere you just need to add how much bit you'll register and the logic of input and output.
![[Screenshot from 2025-09-10 22-53-52.png]]
# 3.4.2 State Encodings
Remember that we have choose the output encodings arbitrarily. Different choices, different answers. How can we determine the encoding that produces the circuit with the fewest logic gates or the shortest propagattion delay. There is no simple way, however, there is a method to make a decent one. 
Computer-aided design (CAD) tools are also good ar searching the set of possible encoding and select a reasonable one.
What would you choose, *binary enconding* or one-hor encoding. With *binary encoding*, as was used in the traffic light controller example, each state is represented as binary number.
A system with $K$ states only needs $\log_{2}K$ bits of state.
In *one-hot encoding*, a separate bit of state is used for each state. Just one bit is "hot" or TRUE at any time. 
For example, a one-hot encoded FSM with three states would have state encodings of 001, 010, and 100. Each bit of state is stored in a flip-flop, so one hot encoding, the next-state and output logic is often simpler, so fewer gates are required.
This example is interesting.
<span style="color:	#87CEEB">Example 3.6 FSM STATE ENCODING</span>
Let's do a *divide-by-N* counter, that has one output and no inputs.
The output $Y$ is HIGH for one clock cycle out of every N. The output divides the frequency of the clock by $N$.
![[Screenshot from 2025-09-11 10-29-11.png]]
This is the abstract definition. Since there is no output, it always goigng to continue on each clock. 1 00, 100, 100. Which concordates with the scheme.
The next state depends only on the current state. 
![[Screenshot from 2025-09-11 10-48-52.png]]

The output table is your homework. 
The next-state and output equations are:
$$
\begin{align}
S_{1}' = \bar{S_{1}} S_{0} \\
S_{0}' = \bar{S_{1}} \bar{S_{0}} \\
Y = \bar{S_{1}} \bar{S_{0}}
\end{align}
$$
However this is using two bites encoder.
The one-hot encoding uses three bits of state. The next-state and output equations are as follows.
$$
\begin{align}
S_{2}' = S_{1} \\
S_{1}' = S_{0} \\
S_{0}' = S_{2} \\
Y = S_{0}
\end{align}
$$
A related encoding is the *one-cold* encoding, in which $K$ states are represented with $K$ bits, exactly one of which is FALSE.
# 3.4.3 Moore and Mealy Machines
Moore machines the output depends only on the state of the system. Mealy machines are much like Moore machines, but the outputs can depend on inputs as well as the current state. Mealy machines, the outputs are labeled on the arcs instead of in the circles.
The block of combinational logic that computes the outputs uses the current state and inputs.
<span style="color:	#87CEEB">Example 3.7 Moore versus mealy machine</span>
There is a snail which every time he reads 01 as the last bits, it smiles. Design the FSM machine, for which the snail smiles. The snail reads bit by bit. Compare moore versus mealy machine. The output is true when the snail smiles.
![[Screenshot from 2025-09-14 09-50-00.png]]
Here is the resume.
The mealy machine requires only two states, Each arc is labeled as $A/Y$. A is the value of the input that causes that transition, and $Y$ is the corresponding output. 
From these tables, we find the next state and output equations by inspection. These equations are simplifies using the fact that state 11 does not exist. Thus, the corresponding next state and output for the non-existent state are don't cares. 
Now the tables you saw, were the state transition tables, it means, if the current state is S0 with  a A = 0, input it will go to S1. Now, the output table says that if you are in a specific state, the output will be 0 or 1.
To know the equations we need to take a look to the next.
![[Screenshot from 2025-09-14 10-12-16.png]]

$$
\begin{align}
S_{1}'  & = S_{0} A \\
S_{0}'  & = \bar{A}
\end{align}
$$
$$
Y = S_{1}
$$
With the moore state transition table we'll have the current state and the output. Just looking at it, you'll see the equation easily. If not, just make the K map.
# 3.4.4 Factoring State Machines
We are going to apply hierarchy and modularity, to factor state machines. Complex FSMs can be broken into multiple easy parts, and therefore we know the output, and therefore, it's easy to built.
For the last problem:
![[Screenshot from 2025-09-14 10-16-22.png]]
There is a good example in the book, when you are in parade mode or not. Please check that.
# 3.4.5 Deriving an FSM from a Schematic
To derive the state transition diagram from a schematic it's the opposite process.
We could need that when a project from other people is incomplete and we need to do reverse engineering.
<span style="color:	#87CEEB">Example 3.9</span>
Hacker does not remember his pin, but casually (xd) she has a circuit design to make her door open. Dammit she does not remember it's two digit password, but she could know it, analizing the circuit and transforming into a state machine. The figure is the next.
![[Screenshot from 2025-09-14 10-36-23.png]]
This is a moore machine. Because the output depends only on the current state.
The equations are simple to know:
$$
\begin{align}
S_{1}' = S_{0} \bar{S_{1}} A_{0}   \\
S_{0}' = \bar{S_{1}} \bar{S_{0}} A_{1} A_{0} \\
\text{Unlock } = S_{1}
\end{align}
$$

![[Screenshot from 2025-09-14 10-46-12.png]]

She assigns names to each state bit combination: $S0$ is $S_{1:0}$, $S 1$ is $S_{1:0} = 01$, and $S_{2}$ is $S_{1:0} = 10$.
WIth that information, you could easily make the next.
![[Screenshot from 2025-09-14 10-48-02.png]]
The door is open!!
# 3.4.6 FSM Review
To design a FSM take the next in cound.
-> Identify the inputs and outputs
-> Sketch a state transition diagram
-> For a moore machine:
 - Write a state transition table
 - Write an output table
-> For a mealy machine
 - Write a combined state transition and output table.
-> Select state encodings- your selection affects the hardware design.
-> Write boolean equations for the next state and output logic.
-> Sketch the circuit schematic.
Excercises
