On this chapter we will analyze and design *sequential* logic. The outputs of sequential logic depend on bot current and prior input values.
SEQUENTIAL LOGIC HAS MEMORY
The state of a digital sequential circuit is a set of bits called *state variables* that contain all the information about the past necessary to explain the future behavior of the circuit.
___
# Latches and flip-flops
The fundamental building block of memory is a *bistable element* -> An element with two stable states.
The most basic form is:
![[Screenshot from 2025-09-07 19-04-55.png]]
The inverters are *cross-coupled*, meaning that the input of I1 is the output of I2 and vice versa. The circuits has no inputs but it does have two outputs, $Q$ and $\bar{Q}$.
In order to analyze this, we need to cyclic, because the circuit is dependent.
Considering two cases.
**Case 1: Q = 0**
I2 recieves a FALSe input, Q, so it produces a TRUE output, $\bar{Q}$. I1 receives a TRUE input, $\bar{Q}$, so it produces a FALSE output on $Q$. This is consistent with the original assumption that $Q = 0$, so the case is said to be *stable*.
**Case 2: Q = 1**
Analogously, this is also stable.
SInce, the cross-coupled have two stable states, the circuit is said to be bistable.
>[!warning] Another option
>There is a third option or possible state, with both outputs approximately halfway between 0 and 1. This is called a *metastable* state.

>[!tip] Carry of information
>An element with N stable states conveys $\log_{2} N$ bits of information

A bistable element stores one bit.
The value Q tell us everything about the past of this circuit, because it will remain forever with the same value whether is 1 or 0.
This was fun, but unpractical, because we have no inputs, and no control, then, let's add more control to our lives!!!
# 3.2.1 SR Latch
It's composed of two cross-couples NOR gates. It has two inputs $S,R$ and the same outputs.
It's state can be controlled through the S and R inputs, which *set* and *reset* the output $Q$.
![[Screenshot from 2025-09-07 19-17-31.png]]
Let's analyze it!
**Case 1: R= 1, S = 0**:
N1 sees at least one TRUE input, R, so it produces a FALSE output on Q. N2 sees both $Q$ and $S$ FALSE, so it produces a TRUE output on $\bar{Q}$.
**Case 2: R= 0, S = 1**:
N1 receives inputs of 0 and $\bar{Q}$. Because we don't yet know $\bar{Q}$, we can't determine the output $Q$. N2 receives at least one TRUE input, S, so it produces a FALSE output on $\bar{Q}$. Now we can revisit N1, knowing that both inputs are FALSE, so the output $Q$ is TRUE.
**Case 3: R= 1, S = 1**
N1 and N2 both see ar least one TRUE input (R or S), so each produces a FALSE output. Hence $Q$ and $\bar{Q}$ are both FALSe.
**Case 4: R= 0, S = 0**
WE don't know? But we could know it checking what happens in each of these subcases.
**Case 4.1: R= 0, S = 0**
Because $S$ and $Q$ are FALSE, N2 produces a TRUE output on $\bar{Q}$. Noe N1 receives one TRUE input, $\bar{Q}$, so its output, Q is FALSE, just as we had assumed.
**Case 4.2: R= 0, S = 0**
Because $Q$ is TRUE, N2 produces a FALSE output on $\bar{Q}$. NOw N1 receives two FALSe inputs, R and $\bar{Q}$, so its output, Q, is TRUE just as we had assumed.
Putting this all together, suppose $Q$ has some known prior value, which we will call $Q_{prev}$, before we enter CAse Iv. $Q_{prev}$ is either 0 or 1, and represents the state of the system. When $R$ and $S$ are 0, $Q$ will remember this old value, $Q_{prev}$, and $\bar{Q}$ will be its complement, $\bar{Q}_{prev}$. This circuit has memory.
![[Screenshot from 2025-09-07 19-32-08.png]]
Using the symbol is an application of abstraction and modularity. There are various
ways to build an SR latch, such as using different logic gates or transis-
tors.
WE CAN STORE A F* (FENOMENAL) BIT
# 3.2.2 D Latch
The problem with the last way to store a bit is that asserting one of the inputs determines not only what the state should be but also when it should change. The D latch separate these problems.
It has two inputs. The data input, D, controls what the next state should be. THe clock input, CLK, controls when the state should change.
Again we analyze with the truth table.
For convenience, we first consider the internal nodes $\bar{D},S,$ and R. If CLK = 0, both $S$ and $R$ are FALSE, regardless of the value of D. If CLK = 1, one AND gate will produce TRUE and the other FALSE, depending on the value of D. Given S and R, Q and $\bar{Q}$ are determined using what we see. Observe that when CLK = 0, $Q$ remembers its old value, $Q_{prev}$. When CLK = 1, $Q = D$. In all cases, $\bar{Q}$ is the complement of $Q$, as would seem logical. The $D$ latch avoids the strange case of simultaneously asserted $R$ and $S$ inputs.
The clock controls when data flows through the latch. WHen CLK = 1, the latch is transparent. The data at D flows through to $Q$ as if the latch were just a buffer. When CLK = 0, the latch is *opaque*. It blocks the new data from flowing through to Q, and $Q$ retains the old value. Hence, the D latch is sometimes called a *transparent latch* or a *level-sensitive* latch. The $D$ latch symbol is:
![[Screenshot from 2025-09-07 19-59-16.png]]
# 3.2.3 D FLIP-FLOP
This flip flop is similar to the other D Latch. However, the difference is simple. When you go from 0 to 1, It will store the value, in other case it will not. 
A D flip-flop can be built from two back-to-back D latches controlled by complementary clocks. The first latch, L1, is called the master. The second latch, L2 is called the slave. (I have no clue why it is) The node between them is named N1. A symbol for the D flip-flop is given. Qhen the $\bar{Q}$ output is not needed, the symbol is often condensed:
![[Screenshot from 2025-09-08 08-41-45.png]]
WHen CLK = 0, the master latch is transparent and the slave is opaque. Therefore, whatever value was at D propagates through to N1. When CLK = 1, the master goes opaque and the slave becomes transparent. The value at N1 propagates through to Q, but N1 is cut off from D. Hence, whatever value was at D inmediately before the clock rises from 0 to 1 gets copied to Q immediately after the clock rises. At all other times, $Q$ retains its old value, because there is always an opaque latch blocking the path between D and Q.
A $D$ flip flp copies D to Q on the rising edge of the clock, and remembers its state at all other times. 
The rising edge of the clock is often just called the clock edge for brevity. THe D input specifies what the new state will be.
The triangle in the symbols denotes an edge-triggered clock input. 
# 3.2.4 Register
An $N-bit$ register is a bank of $N$ flip-flops that share a common CLK input, so that all bits of the register are updated at the same time. 
![[Screenshot from 2025-09-08 08-52-12.png]]

# 3.2.5 Enabled Flip-Flop
An *enabled flip-flop* adds another input called EN or ENABLE to determine whether data is loaded on the clock edge. When EN is TRUE, the enabled flip-flop behaves like an ordinary D flip-flop. When EN is FALSE, the enabled flip-flop ignores the clock and retains its state.
This is useful when we wish to lead a new value into a flip flop only some of the time.
The way to construct vary. However, a great recommendation is to not work with the clock, because it can carry problems. Add gates to the clock only if you know what you are doing, which is not your case, so don't do it.
![[Screenshot from 2025-09-08 11-33-02.png]]
The first was created with a MUX and the second with and AND gate.
# 3.2.6 Resettable Flip-Flop
A *resettable flip-flop* adds another input "RESET". When RESET is FALSE, the resettable flip-flop behaves like an ordinary D flip-flop. When RESET is TRUE, the resettable flip-flop ignores D and resets the output to 0.
This is useful when you want to reset a lot of flip flops to a know value.
These can be:
- *synchronously resettable*: Reset themselves only on the rising edge of CLK
- *asynchronously resetable*: reset themselves as soon as RESET becomes TRUE, independent of CLK
![[Screenshot from 2025-09-08 11-39-56.png]]
Asynchronously resettable flip-flops require modifying the internal structure, you'll do that in exercises. So, good luck.
# 3.2.7 Transistor-Level Latch and Flip-Flop Designs
Since the fundamental role of a latch is to be transparent or opaque, much like a switch. So, we could (really the author) reduce the transistor count.
A compact D latch can be constructed from a single transmission gate, as shown:
![[Screenshot from 2025-09-08 11-49-03.png]]
In a) when CLK = 1 and $\overline{(CLK)} = 0$, the transmission gate is ON, so D flows to Q and the latch is transparent. When CLK = 0 and $\overline{CLK} = 1$, the transmission gate is OFF, so $Q$ is isolated from D and the latch is opaque. However, even if it's simple, it has some problems:
- *Floating output node*: WHen the latch is opaque, $Q$ is not held at its value by any gates. Thus $Q$ is called a *floating or dynamic node*. After some time, noise and charge leakage may distrub the value of $Q$.
- NO buffers: The lack of buffers has caused malfunctions on several commercial chips. A spike of noise that pulls $D$ to a negative voltage can turn on the nMOS transistor, making the latch transparent, even when $CLK = 0$. LIkewise a spike on D above $V_{DD}$ can turn on the pMOS trnasistor even when $CLK = 0$. The general rule is that neither the input of a transmission gate nor the state node of a sequential circuit should ever be exposed to the outside world, where noise is likely.
Figure b) shows a mroe robus 12-transistor D latch used on modern commercial chips.
#### Other flips flops in excercises.
Exercise 3.9 The toggle (T) flip-flop has one input, CLK, and one output, Q. On
each rising edge of CLK, Q toggles to the complement of its previous value. Draw
a schematic for a T flip-flop using a D flip-flop and an inverter.

Exercise 3.10 A JK flip-flop receives a clock and two inputs, J and K. On the rising
edge of the clock, it updates the output, Q. If J and K are both 0, Q retains its old
value. If only J is 1, Q becomes 1. If only K is 1, Q becomes 0. If both J and K are 1,
Q becomes the opposite of its present state.
# 3.2.8 Putting it all togueter
Remember that a D latch is level-sensitive, whereas a D flip-flop is edge triggered. The D latch is transparent when $CLK = 1$, allowing the input $D$ to flow through to the output $Q$.The D flip-flop copies $D$ to $Q$ on the rising edge of CLK. At all other times, latches and flip-flops retain their old state. A register is a bank of several D flip-flops that share a common $CLK$ signal.

