created: 2026-01-14
**source:** [[B-Introduction-to-algorithms - Cormen]]
**tags:** #book_chapter 
## Resumen 
To build a heap from an unordered array, we need to take some properties first. Let's called that $\left\lfloor  \frac{n}{2}  \right\rfloor +1$ to $n$ elements of a heap are leafs.  
[[E-Procedure Build-Max-Heap]]
Now, we want to assure that te procedure holds, therefore we need the correctness of the algorithm and the loop invariant.
[[E- Loop invariant for Build-Max-Heap Procedure]]
[[E-Running time for Build-Max-Heap Procedure]]
## Referencias a notas permanentes
